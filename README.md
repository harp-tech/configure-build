# configure-build

Action for configuring Harp device project builds using the Bonsai Foundation standard versioning and release scheme from GitHub Actions workflows. Specifically, the device metadata file `device.yml` is taken into account when validating and configuring version numbers.
